## What does this PR do?

<!-- Describe the change: adding a new entry, fixing metadata, adding a chain, etc. -->

## Checklist

- [ ] Entry JSON is valid (`pnpm validate` passes)
- [ ] File is properly formatted (`pnpm format:check` passes)
- [ ] File is placed at the correct path (`registry/<vm_type>/<chain_slug>/<address>.json`)
- [ ] At least one `block_explorer` source is included
- [ ] Contract address is lowercase (EVM)
